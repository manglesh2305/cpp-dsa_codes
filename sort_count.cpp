#include<iostream>
using namespace std;
void count_sort(int* A, int n)
{
    int max=A[0];
    for(int i=1; i<n; i++)
    {
        if(A[i]>max)
        {
            max=A[i];
        }
    }
    int arr[max];
    for(int i=0; i<n; i++)
    {
        
    }
}